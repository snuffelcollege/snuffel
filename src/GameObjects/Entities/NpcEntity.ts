import MovableEntity from "./MovableEntity";

/**
 * @deprecated
 * Use components instead
 * @remarks
 * An NpcEntity is a subclass of MovableEntity and will be used to render
 * non player characters inside the game. This entity can be used to render either
 * humanoids or dogs.
 */
export default class NpcEntity extends MovableEntity {}
